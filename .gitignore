# Credentials & secrets
.env
input.json
input_foreclosure_daily.json

# State files (generated at runtime)
last_run.json
seen_ids.json
captcha_failed_ids.json
.ancestry_page_loads.json
cookies.json
datasift_cookies.json
datasift_api_tokens.json
dropbox_state.json
photo_state.json
# SmrtPhone dialer session cookies (minted by `python src/smrtphone.py login`)
smrtphone_state.json
*.tmp
*.bak

# Output (generated data)
output/
logs/
data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Browser profiles (contain login data)
.ancestry_profile/
.datasift_profile/
.playwright/

# Playwright debug screenshots (from DataSift uploader runs)
datasift_*.png

# Apify local storage
apify_storage/
storage/

# Test data & photos
test_photos/

# Skills distribution ZIPs (large, distributed separately)
Skills for REI/*
# ...except the DirectSkip skill SOURCE (SKILL.md + references + scripts),
# tracked so it syncs across devices; the packaged ZIPs stay out via *.skill
!Skills for REI/directskip-datasift-clean/
*.skill
*.plugin

# IDE & OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Claude Code / planning
.claude/
.planning/
.playwright-mcp/

# Misc
node_modules/
lists-hub/
_brand_tmp/
_tmp_skill/
autoresearch/
.pytest_cache/

# Local scratch / backups (do not commit or deploy)
state_backups/
probe_datasift_routes.py

# Local transcript/playbook working folders (large, never deployed/committed)
*Transcripts/
*Transcripts.log
*Playbook/
Curative Title Playbook/
youtube_transcripts.py
# Timestamped lead-data backups (PII) + purge/poll logs — never commit
*_backup_*.csv
owner_purge_poll.log
purge_all_*.log
